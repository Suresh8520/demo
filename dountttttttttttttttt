pipeline {
    agent any

    stages {
        stage('Download') {
            steps {
                script {
                    def repoUrl = 'https://github.source.internal.cba/ss99/powersell/archive/refs/heads/main.zip'
                    sh "wget $repoUrl"
                }
            }
        }
    }
}
