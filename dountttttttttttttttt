$gitZipUrl = "https://your_internal_git_repo.com/path/to/your/zipfile.zip"
$outputFilePath = "C:\path\to\destination\file.zip"

# If your repository requires authentication, you can use the following code:
#$username = "your_username"
#$password = "your_password"
#$cred = New-Object System.Management.Automation.PSCredential($username, (ConvertTo-SecureString $password -AsPlainText -Force))
#Invoke-WebRequest -Uri $gitZipUrl -Credential $cred -OutFile $outputFilePath

# If your repository does not require authentication, use the following code:
Invoke-WebRequest -Uri $gitZipUrl -OutFile $outputFilePath

Write-Host "ZIP file downloaded successfully!"
